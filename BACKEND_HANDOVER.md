# バックエンド開発チーム向け引継ぎ資料

## 1. プロジェクト概要

本プロジェクトは、顧客がウェブサイト上でAIチャットボットとの対話を通じて、自社が求める設備（工場の自動機・半自動機）の簡易的な仕様書（提案書）を自動生成できるツールを開発するものです。
主な目的は以下の通りです。

*   設備仕様書の作成に不慣れな顧客でも、容易に初期検討を進められるようにする。
*   AIとの対話を通じて、顧客の潜在的なニーズや課題を明らかにする。
*   自動生成された簡易提案書をフックとして、営業リードを獲得し、その後の詳細ヒアリングや商談に繋げる。

## 2. システム構成（案）

```
+-----------------+     HTTP      +---------------------+     Gemini API     +-----------------+
|  フロントエンド   | <---------> | バックエンド (Python) | <--------------> |   Google Gemini |
| (HTML,CSS,JS)   |             |  (Flask/FastAPI)    |                |     (LLM)       |
+-----------------+             +----------+----------+                +-----------------+
                                           |
                                           | DB (PostgreSQL)
                                           v
                                     +-----------------+
                                     |   データベース    |
                                     | (会話ログ, 提案) |
                                     +-----------------+
```

*   **フロントエンド:** HTML, CSS, JavaScript で構築されたウェブインターフェース。顧客とのチャットUI、簡易提案書の表示、問い合わせフォームを提供します。（既にモックアップ版が `main` ブランチにあります）
*   **バックエンド:** Python (Flask または FastAPI を想定) で構築されるAPIサーバー。
    *   フロントエンドからのチャットメッセージを受け付け、Google Gemini API と連携して応答を生成します。
    *   会話の文脈を理解させるため、Gemini API に渡すプロンプトには、AIの役割設定、目標、行動指針、および直近の会話履歴を含めます。
    *   AIとの会話ログ、AIが抽出した顧客の要求事項、最終的に生成された簡易提案書の内容などをデータベース（PostgreSQLを想定）に保存します。
    *   （将来的には）管理者向けのデータ閲覧インターフェースを提供することも視野に入れます。
*   **Google Gemini API:** 中核となる大規模言語モデル(LLM)。自然な対話、顧客の意図理解、提案内容の生成を行います。
*   **データベース:** PostgreSQL を想定。会話ログ、抽出された仕様情報、顧客情報、生成された提案書データなどを格納します。

## 3. バックエンドAPI仕様案

### 3.1. チャットメッセージ送受信API

*   **エンドポイント:** `/api/chat`
*   **HTTPメソッド:** `POST`
*   **目的:** フロントエンドからのユーザーメッセージを受け取り、Gemini API を介してAIの応答を生成し、フロントエンドに返す。同時に会話ログを保存する。
*   **リクエストボディ (JSON):**
    ```json
    {
        "message": "<ユーザーが入力したメッセージ文字列>",
        "currentSessionHistory": [
            {"role": "user", "parts": [{"text": "<このセッションでの直近のユーザー発言1>"}]},
            {"role": "model", "parts": [{"text": "<このセッションでの直近のAI応答1>"}]}
            // ... このセッション内のこれまでの会話履歴（適度な量に制限）
        ]
    }
    ```
*   **レスポンスボディ (JSON):**
    ```json
    {
        "aiResponse": "<Gemini APIからの応答メッセージ文字列>",
        "extractedInfo": {
            "purposeAndWork": "<抽出された目的とワーク>",
            "taktAndAutomation": "<抽出されたタクトタイムと自動化レベル>",
            "mainProcess": "<抽出された主要加工や検査内容>",
            "environmentAndUtilities": "<抽出された設置環境とユーティリティ>",
            "issuesAndBudget": "<抽出された課題と予算感>",
            // ... その他、AIが抽出・判断した情報
            "isComplete": false // ヒアリングが完了したかどうかのフラグ (trueなら提案書作成へ)
        }
    }
    ```
*   **主な処理内容:**
    1.  リクエストボディからユーザーメッセージと会話履歴を取得。
    2.  **システムプロンプト（後述）** と、受信した会話履歴、ユーザーメッセージを組み合わせて、Gemini API に送信するプロンプトを構築。
    3.  Gemini API を呼び出し、応答を取得。
    4.  Gemini API の応答から、フロントエンドに表示する `aiResponse` と、構造化された `extractedInfo` を抽出・整形。
        *   `extractedInfo` の各項目は、会話が進むにつれてAIが情報を得られた場合に更新されていきます。
        *   `isComplete` フラグは、AIが主要5項目（目的/ワーク、生産量/自動化、主要加工/機能、設置環境/ユーティリティ、課題/予算）について十分な情報を得られたと判断した場合に `true` となります。
    5.  ユーザーメッセージ、AI応答、タイムスタンプ、セッション識別子（内部で管理）などをデータベースの会話ログテーブルに保存。
    6.  抽出された `extractedInfo` も適宜データベースに保存・更新。
    7.  整形したレスポンスボディをフロントエンドに返す。

### 3.2. システムプロンプト（Gemini APIへの初期指示）の骨子

Gemini API が期待される役割（AI設備仕様コンシェルジュ）を適切に果たせるよう、API呼び出しの際に以下のような指示を会話の前提として含める必要があります。

```text
あなたは、工場の自動機・半自動機の導入を検討している企業の担当者から、設備仕様に関するヒアリングを行うAIアシスタント「AI設備仕様コンシェルジュ」です。
あなたの目的は、以下の5つの主要な項目について、丁寧かつ専門的にユーザーから情報を引き出すことです。

1.  **目的とワーク（製品）:** どのような目的の設備で、何を扱いたいか。
2.  **生産量（タクトタイム）と自動化レベル:** 1時間あたりの目標生産量、または1つあたりの目標タクトタイム。設備の自動化レベル（全自動、半自動、手動）の希望。
3.  **主要な加工や検査の内容と重要な機能:** 最も中心となる加工や検査は何か。その他に必須または重要な機能は何か。
4.  **設置場所の環境とユーティリティ:** クリーンルームの有無、広さ、搬入経路、電源（単相100V, 三相200Vなど）、エア圧の状況など。
5.  **現状の課題と期待、おおよその予算感:** 現在抱えている課題、設備導入で期待する解決、現時点での大まかな予算。

会話の進め方:
- ユーザーに対して親しみやすく、専門用語を使いすぎない平易な言葉で質問してください。
- 一度に多くのことを聞かず、1〜2つの質問を簡潔に投げかけてください。
- ユーザーの回答が曖昧な場合は、具体例を提示したり、より掘り下げた質問をしたりして、情報を明確にしてください。
- ユーザーがITツールに不慣れな場合も想定し、分かりやすい説明を心がけてください。
- 最終的に上記5項目が概ね明らかになったと判断したら、ヒアリングが完了した旨を伝え (`isComplete: true` を返す)、次のステップ（提案書の確認、担当者からの連絡）について案内してください。
- あなたの応答は、常にJSON形式の `aiResponse` (ユーザーへの表示メッセージ) と `extractedInfo` (抽出情報オブジェクト) を含むようにしてください。`extractedInfo`には上記5項目に対応するキーと、それぞれの情報ステータスや抽出内容、そして全体としてヒアリングが完了したかを示す `isComplete` (boolean) を含めてください。

さあ、最初の挨拶をして、ユーザーにどのような設備を検討しているか尋ねてください。
```
**注意:** このシステムプロンプトはあくまで骨子であり、実際の応答品質を見ながら継続的な改善（プロンプトエンジニアリング）が必要です。特に `extractedInfo` の内容を安定して構造的に出力させるためには、Gemini API の Function Calling 機能の利用も検討する価値があります。

### 3.3. （簡易提案書表示ページ向け）データ連携

フロントエンドの `result.html` は、チャット完了時に `localStorage` に保存された `extractedInfo` を読み込んで簡易提案書の内容を表示します。
バックエンドとしては、`extractedInfo.isComplete` が `true` になった時点で、フロントエンドがこの情報を元に `result.html` に遷移することを想定しています。
データベースには、この `extractedInfo` の最終状態が提案内容として保存されている必要があります。

### 3.4. お問い合わせフォームデータの保存

`result.html` にある「詳細ヒアリングお申し込みフォーム」から送信されるデータ（氏名、会社名、メールアドレス、電話番号など）を受け取り、データベースの顧客情報テーブルに保存するAPIエンドポイントも必要です。（例: `/api/contact`）

## 4. データベース設計案（主要テーブル）

*   **Users (または Customers):** 顧客情報
    *   `user_id` (PK)
    *   `name` (氏名)
    *   `company_name`
    *   `email`
    *   `phone_number` (任意)
    *   `created_at`
*   **ChatSessions:** AIとの一連の会話セッション
    *   `session_id` (PK)
    *   `user_id` (FK, フォーム送信前はNULLの場合あり)
    *   `start_time`
    *   `end_time` (ヒアリング完了時)
    *   `final_proposal_summary` (JSONB, 最終的な `extractedInfo` など)
*   **ChatLogs:** 個々の会話のやり取り
    *   `log_id` (PK)
    *   `session_id` (FK)
    *   `sender` ("user" または "ai")
    *   `message_content` (TEXT)
    *   `timestamp`
    *   `raw_gemini_request` (JSONB, デバッグ用)
    *   `raw_gemini_response` (JSONB, デバッグ用)
*   **ExtractedInformation:** AIが会話から抽出した構造化情報（セッションごとに最新のものを保持、または変更履歴を追うか検討）
    *   `info_id` (PK)
    *   `session_id` (FK)
    *   `purpose_and_work` (TEXT)
    *   `takt_and_automation` (TEXT)
    *   `main_process_and_functions` (TEXT)
    *   `environment_and_utilities` (TEXT)
    *   `issues_and_budget` (TEXT)
    *   `is_complete` (BOOLEAN)
    *   `updated_at`

上記はあくまで初期案であり、詳細なカラムやリレーションシップは開発を進める中で調整してください。

## 5. 技術スタック（推奨）

*   **プログラミング言語:** Python (3.8以上)
*   **フレームワーク:** Flask または FastAPI
    *   Flask: シンプルで学習コストが低い。小～中規模APIに適している。
    *   FastAPI: 高速で、型ヒントによるデータ検証や自動APIドキュメント生成機能が強力。非同期処理にも対応。
*   **データベース:** PostgreSQL
*   **Gemini API Client:** Google の Python クライアントライブラリ (`google-generativeai`)
*   **その他ライブラリ:**
    *   `python-dotenv`: 環境変数管理 (APIキーなど)
    *   `psycopg2-binary` (または `asyncpg` FastAPIで非同期利用の場合): PostgreSQLアダプタ

## 6. 環境構築と実行

*   オンプレミスサーバーでの実行を想定。
*   Pythonの仮想環境 (venv や conda) の利用を推奨。
*   必要なライブラリは `requirements.txt` で管理。
*   Gemini API キーは環境変数 (`GOOGLE_API_KEY`) などで安全に設定。
*   WSGIサーバー (Gunicornなど) を介してアプリケーションをデプロイ。

## 7. 開発の進め方・注意点

1.  **Gemini API連携の確立:** まず、基本的なチャットの送受信とGemini APIからの応答取得を実装します。
2.  **プロンプトエンジニアリング:** システムプロンプトを調整し、AIが期待通りの質問をし、情報を引き出せるようにします。`extractedInfo` の精度向上が鍵となります。
3.  **データベース連携:** 会話ログと抽出情報の保存処理を実装します。
4.  **エラーハンドリングとロギング:** APIエラー、Gemini APIからの予期せぬ応答などに対応できる堅牢なエラーハンドリングと、デバッグや分析のための適切なロギングを実装します。
5.  **セキュリティ:** APIキーの管理、入力値のバリデーションなど、基本的なセキュリティ対策を講じます。
6.  **フロントエンドとの連携テスト:** フロントエンドチームと密に連携し、APIの動作確認を行います。

## 8. フロントエンドの現状 (引継ぎ時点)

*   `index.html`: トップページ/サービス紹介
*   `hearing.html`: AIチャットUI。現在はJavaScriptによるダミー応答。`/api/chat` を呼び出す準備はできています（URLは `http://localhost:5000/api/chat` 固定）。
*   `result.html`: 簡易提案書表示ページ。`localStorage` からチャット結果を読み込んで表示。
*   `thankyou.html`: 問い合わせ完了ページ。
*   `mock_server.py`: `hearing.html` のAPI呼び出し先として機能する、Python Flask製の超簡易モックサーバー。フロントエンドの動作確認用です。

この `mock_server.py` のロジックを発展させ、Gemini API連携やデータベース保存機能を追加していく形でバックエンドを構築していくイメージです。

ご不明な点があれば、お気軽にご質問ください。
